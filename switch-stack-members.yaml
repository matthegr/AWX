---
- name: Show Version
- hosts: LISD IDF3 3850
  gather_facts: false
  connection: local
  
  vars:
    cli:
      username: admin
      password: LEcisc33e$
      timeout: 100
      
  tasks:
   - name: Verify ACL is present
     ios_command:
      provider: "{{ cli }}"
      lines:
         - sh access-l

   - name: SAVE CONFIG
     ios_config:
       authorize: yes
       save: yes
